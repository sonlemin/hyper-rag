"""Công cụ chạy tay của dự án, gọi bằng `python -m scripts.<tên>`.

Chỉ những script mà kết quả của nó đi vào khóa luận mới nằm ở đây, và khi đã
nằm ở đây thì nó có test như mọi tầng khác. Bản shell (`chay-may-chu.sh`,
`ci-remote.sh`) vẫn là file thực thi cạnh gói này.
"""
